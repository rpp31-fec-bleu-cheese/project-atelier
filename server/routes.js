/*
const router = require('express').Router();
const controller = require('./controllers.js');

// routes for products
router
  .route('/products')
  .get(controller.products.get)

// routes for reviews
router
  .route('/reviews')
  .get()

router
  .route('/reviews/meta')
  .get()
  .post()

// routes for questions and answers
router
  .route('/qa/questions')
  .get(controller.questions_answers.get)
  .post(controller.questions_answers.post)

// routes for list of products added to cart
router
  .route('/cart')
  .get()

module.exports = router;
*/