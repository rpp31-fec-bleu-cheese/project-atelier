import React from 'React';
import PropTypes from 'prop-types';

const WriteButton = props => {
  return (
    <div id='WriteButton' className='Button'>
      WRITE REVIEW
    </div>
  );
};

export default WriteButton;